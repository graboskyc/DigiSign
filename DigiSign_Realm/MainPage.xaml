<Page
    x:Class="DigiSign_Realm.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DigiSign_Realm"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="#FF000000" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <StackPanel Name="ctr_configstack" Orientation="Vertical" VerticalAlignment="Stretch">
            <TextBlock Foreground="White" Margin="0,50,0,0" FontSize="30">Sign list is null, zero length, or device not provisioned</TextBlock>
            <TextBox Name="txt_deviceID" Text="" Foreground="White" Margin="0,10,0,0"></TextBox>
            <TextBox Name="txt_error" Text="" Foreground="White" Margin="0,10,0,0"></TextBox>
        </StackPanel>
        <Image x:Name="ctr_view_image" Visibility="Collapsed" />
        <WebView x:Name="ctr_view_web" Visibility="Collapsed"/>
        <RichEditBox Name="ctr_view_text" FontSize="100" Foreground="White" TextAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"></RichEditBox>
        <MediaElement x:Name="ctr_view_media" AutoPlay="True" AreTransportControlsEnabled="False" Visibility="Collapsed" IsLooping="True" />
    </Grid>
</Page>
